wso2:
  apim:
    # Node 2: port offset 1 to avoid port conflicts with node1
    portOffset: 1
    configurations:
      # Node 2 is the secondary node — skips admin account creation
      # and sets throttling peers pointing to node1
      extraConfigs: |
        [super_admin]
        create_admin_account = false

        [apim.throttling]
        event_duplicate_url = ["tcp://apim-node1:5672"]
        throttle_decision_endpoints = ["tcp://apim-node2:5673"]

        [[apim.throttling.url_group]]
        traffic_manager_urls = ["tcp://apim-node1:9611"]
        traffic_manager_auth_urls = ["ssl://apim-node1:9711"]
        type = "loadbalance"

        [[apim.throttling.url_group]]
        traffic_manager_urls = ["tcp://apim-node2:9612"]
        traffic_manager_auth_urls = ["ssl://apim-node2:9712"]
        type = "loadbalance"
